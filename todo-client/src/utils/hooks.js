import { toast } from 'react-toastify';
import customFetch from './customFetch';

export const addTaskHandler = async (e) => {
  console.log(e);
  //   try {
  //     await customFetch('/task').post(data);
  //     toast.success('task created');
  //   } catch (error) {
  //     toast.error('something went wrong');
  //   }
};
